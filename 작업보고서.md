# 프로젝트 일정 관리 엑셀 템플릿 작업 보고서

## 작업 일시
2026-03-03

## 작업 개요
프로젝트 작업별 시작/종료 일자를 바탕으로 실제 근무일(Working Days)을 계산하고 일정 누락 여부를 점검하는 엑셀 템플릿을 제작하였습니다.

## 작업 결과물
**파일 위치:** `D:\workspace\schedule\프로젝트일정관리_템플릿.xlsx`

## 구현 내용

### 1. 일정관리 시트 (메인 시트)
#### 컬럼 구성
- **Column A:** 대분류
- **Column B:** 중분류  
- **Column C:** 프로그램ID
- **Column D:** 작업명
- **Column E:** 작업자
- **Column F:** 시작일자 (YYYY-MM-DD 형식)
- **Column G:** 종료일자 (YYYY-MM-DD 형식)
- **Column H:** 실제근무일 (자동 계산)

#### 핵심 기능
- **실제 근무일 계산 수식:** `=NETWORKDAYS(F2,G2,Holidays!$A:$A)`
  - 시작일자와 종료일자 범위 내에서 주말(토/일)과 법정공휴일을 제외한 순수 작업 일수를 자동 계산
  - Holidays 시트에 정의된 공휴일 목록을 참조

### 2. Holidays 시트
- 법정공휴일 목록 관리 시트
- 2026년도 한국 법정공휴일 포함:
  - 신정 (01-01)
  - 3.1절 (03-01)
  - 어린이날 (05-05)
  - 현충일 (06-06)
  - 광복절 (08-15)
  - 개천절 (10-03)
  - 한글날 (10-09)
  - 크리스마스 (12-25)

### 3. 누락날짜시트
- 작업자별 일정 누락 확인을 위한 시트
- 작업자명 입력 셀 제공 (B3)
- 사용 가능한 수식 예시 제공:
  - `=MINIFS(일정관리!$F:$F,일정관리!$E:$E,$B$3)` - 작업자의 최소 시작일 조회
  - `=MAXIFS(일정관리!$G:$G,일정관리!$E:$E,$B$3)` - 작업자의 최대 종료일 조회

### 4. 샘플 데이터
- 3명의 작업자 (작업자1, 작업자2, 작업자3)
- 각 작업자별 10개 작업 할당
- 작업 기간: 2026년 3월 3일 ~ 5월 31일
- 작업 순서: 임의로 배치 (실제 일정 순서대로 정렬되지 않음)
- 총 30개의 샘플 작업 데이터 생성
- 작업 기간: 2~8 근무일 (랜덤)
- 주말을 고려하여 연속적으로 일정 배정

## 기능 검토 사항

### 일정 자동 갱신 방안
**요구사항:** 특정 작업의 시작일/종료일 변경 시 이후 작업들의 일정을 자동으로 갱신

**검토 결과 및 제안:**

#### 방안 1: VBA 매크로 활용 (권장)
- 일정 변경 이벤트를 감지하여 자동으로 이후 작업 일정을 재계산
- 장점: 가장 유연하고 강력한 방법
- 단점: 매크로 사용에 대한 보안 경고 발생 가능

#### 방안 2: 수식 기반 자동 갱신
- 시작일자를 이전 작업의 종료일자 + 1일로 수식 설정
- 예: `=이전행_종료일자+1+IF(WEEKDAY(이전행_종료일자+1)=7,2,IF(WEEKDAY(이전행_종료일자+1)=1,1,0))`
- 장점: 매크로 없이 구현 가능
- 단점: 작업 순서가 행 순서와 일치해야 함

#### 방안 3: 피벗 테이블 + 파워쿼리 활용
- 작업을 정렬하고 파워쿼리로 자동 갱신 로직 구현
- 장점: 데이터 관리 측면에서 효율적
- 단점: 초기 설정이 복잡함

**추천:** 방안 1 (VBA 매크로)가 가장 실용적이며, 필요시 추가 개발 가능

## 사용 방법

1. **일정 입력:**
   - 일정관리 시트에서 작업 정보 입력
   - 시작일자, 종료일자 입력 시 실제근무일이 자동 계산됨

2. **공휴일 관리:**
   - Holidays 시트에서 공휴일 추가/수정
   - 날짜는 YYYY-MM-DD 형식으로 입력

3. **누락 날짜 확인:**
   - 누락날짜시트의 B3 셀에 작업자명 입력
   - 제공된 수식을 활용하여 작업자의 일정 범위 확인

## 개선 제안

1. **조건부 서식 적용:**
   - 주말 날짜 자동 색상 표시
   - 겹치는 일정 강조 표시

2. **간트 차트 추가:**
   - 시각적 일정 관리를 위한 간트 차트 시트 추가

3. **VBA 매크로 개발:**
   - 일정 자동 갱신 기능
   - 누락 날짜 자동 검출 및 표시 기능

4. **데이터 유효성 검사:**
   - 작업자명 드롭다운 리스트
   - 날짜 형식 자동 검증

## 결론
프로젝트 일정 관리를 위한 기본 엑셀 템플릿이 성공적으로 생성되었습니다. NETWORKDAYS 함수를 활용한 실제 근무일 자동 계산 기능과 공휴일 관리 기능이 구현되었으며, 3명의 작업자에 대한 샘플 데이터가 포함되어 있어 즉시 사용 가능합니다.

---
**작성자:** GitHub Copilot CLI  
**작성일:** 2026-03-03
